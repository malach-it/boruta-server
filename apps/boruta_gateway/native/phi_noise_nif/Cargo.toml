[package]
name = "phi_noise_nif"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "phi_noise_nif"
crate-type = ["cdylib"]

[dependencies]
rustler = "0.38.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
